{
  "name": "vetements",
  "version": "1.0.0",
  "description": "\"# vetements\"",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "(yarn --cwd frontend install -P && yarn --cwd backend install -P) && (concurrently \"yarn --cwd backend dev\" \"yarn --cwd frontend dev\")"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bdegrandmaison/vetements.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/bdegrandmaison/vetements/issues"
  },
  "homepage": "https://github.com/bdegrandmaison/vetements#readme",
  "dependencies": {
    "concurrently": "^6.2.0",
    "yarn": "^1.22.10"
  }
}
